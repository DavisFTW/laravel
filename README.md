to run this
    -install dependencies
    -configure env. vars.
    -generate application key ( php artisan key:generate) 
    -run migrations for DB ( php artisan migrate )
    -seed the database ( REQUIRED FOR categories ) ( php artisan db:seed)
    -run it ( php artisan serve ) 
    -run npm ( npm run dev ) 

    
